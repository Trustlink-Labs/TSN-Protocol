# EVM source: Ethereum Sepolia only. Never commit real keys.
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/replace-me
SEPOLIA_CHAIN_ID=11155111

# Creditcoin CC3 Testnet. This is not Solana localnet or Creditcoin mainnet.
CREDITCOIN_RPC_URL=https://rpc.cc3-testnet.creditcoin.network
CREDITCOIN_CHAIN_ID=102031

# Funded testnet deployer/worker key. Use a throwaway devnet/testnet key.
DEPLOYER_PRIVATE_KEY=0xreplace-me

# Creditcoin-first payout policy. The authorization signer signs the exact
# Solana commitment, payout amount, and fee; the Cranker only submits it.
CREDITCOIN_AUTHORIZATION_SIGNER=
CREDITCOIN_FEE_RECIPIENT=
# Devnet default only; set the production policy explicitly.
CREDITCOIN_FEE_BPS=10
CREDITCOIN_SETTLEMENT_TOKEN=0xreplace-with-creditcoin-stablecoin
CREDITCOIN_LIQUIDITY_ASC_ADDRESS=

# Destination EVM liquidity proof input. The source transaction must emit the
# registered LiquidityAvailable event on a chain supported by Attestcoin.
DESTINATION_SOURCE_RPC_URL=
DESTINATION_SOURCE_CHAIN_KEY=
DESTINATION_ROUTE_ID=
DESTINATION_LIQUIDITY_TX_HASH=

# Attestcoin proof builder for CC3 Testnet.
PROOF_BUILDER_URL=https://proof-gen-api.cc3-testnet.creditcoin.network
SOURCE_CHAIN_KEY=1

# Written by scripts/deploy.ts after deployment.
SEPOLIA_ANCHOR_ADDRESS=
CREDITCOIN_ASC_ADDRESS=

# Optional one-shot worker input. If set, the worker processes only this tx.
ANCHOR_TX_HASH=
